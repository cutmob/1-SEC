name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  go-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Staticcheck
        run: |
          go install honnef.co/go/tools/cmd/staticcheck@latest
          staticcheck -checks='all,-U1000,-ST1000,-ST1005,-S1007,-S1009,-S1039,-SA1019,-SA5011' ./...

      - name: Gosec
        continue-on-error: true
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          gosec -exclude=G101 ./...

      - name: Format check
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "Files not formatted:"
            gofmt -l .
            exit 1
          fi

      - name: Test
        run: go test -race -timeout 120s ./...

      - name: Vulnerability check
        continue-on-error: true
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - name: OSV dependency scan
        continue-on-error: true
        run: |
          go install github.com/google/osv-scanner/v2/cmd/osv-scanner@latest
          osv-scanner scan source -r .

  rust-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libpcap-dev

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            rust/1sec-engine/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('rust/1sec-engine/Cargo.toml', 'rust/1sec-engine/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Format check
        working-directory: rust/1sec-engine
        run: cargo fmt -- --check

      - name: Clippy
        working-directory: rust/1sec-engine
        run: cargo clippy --all-features -- -D warnings

      - name: Build
        working-directory: rust/1sec-engine
        run: cargo build --release --all-features

      - name: Test
        working-directory: rust/1sec-engine
        run: cargo test --all-features

      - name: Rust dependency audit
        continue-on-error: true
        working-directory: rust/1sec-engine
        run: |
          cargo install cargo-audit --locked
          cargo audit
